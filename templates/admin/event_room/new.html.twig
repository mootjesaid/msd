{% extends 'admin/modalBase.html.twig' %}

{% block title %}New Event Room{% endblock %}

{% block body %}
    <div class="m-4 p-4 modal:m-0 modal:p-0 bg-gray-800 rounded-lg">
        <div class="flex justify-between">
            <h1 class="text-xl font-semibold text-white mb-4">Create New eventroom</h1>

            <div class="mr-4">
                <a href="{{ path('app_admin_event_room') }}" class="text-sm font-medium text-white">
                    Back to list
                </a>
                <div class="inline-block">
{#                    {% if form.vars.data.id|default %}#}
{#                        {{ include('voyage/_delete_form.html.twig') }}#}
{#                    {% endif %}#}
                </div>
            </div>
        </div>
        {{ include('admin/event_room/_form.html.twig') }}
    </div>
{% endblock %}

{% block stream_success %}
    <turbo-stream action="prepend" targets="#{{ event_room ~ '-list'}} tbody">
        <template>
            {{ include('admin/_row.html.twig') }}
        </template>
    </turbo-stream>
{% endblock %}