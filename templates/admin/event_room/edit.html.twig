{% extends 'admin/base.html.twig' %}

{% block main %}
    <div class="m-4 p-4 modal:m-0 modal:p-0 bg-gray-800 rounded-lg">
        <div class="flex justify-between">
            <h1 class="text-xl font-semibold text-white mb-4">Edit Voyage</h1>

            <div class="mr-4">
                <a href="" class="text-sm font-medium text-white">
                    Back to list
                </a>
                <div class="inline-block">
                    {% if form.vars.data.id|default %}
{#                        {{ include('event_room/_delete_form.html.twig') }}#}
                    {% endif %}
                </div>
            </div>
        </div>

        {{ include('admin/event_room/_form.html.twig') }}
    </div>
{% endblock %}